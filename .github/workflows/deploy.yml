name: DevOps Pipeline

on:
  push:
    branches:
      - main
    workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Step 18 Debug
        run: echo "Step 18 is working"

      - name: Run a test message
        run: echo "Automation is working"

      - name: Step 19 Manual Test
        run: echo "Manual trigger is working"