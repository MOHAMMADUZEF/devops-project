name: Step 24 - Env & Secrets

on:
  push:

jobs:
  demo:
    runs-on: ubuntu-latest

    env:
      APP_NAME: MyApp

    steps:
      - name: Print Environment Variable
        run: echo "App name is $APP_NAME"

      - name: Use Secret
        run: echo "Secret is ${{ secrets.MY_SECRET }}"